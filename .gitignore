# Supabase config is committed so the live site (GitHub Pages) can create share links.
# The anon key is safe for frontend use; Supabase uses RLS to protect your data.
# supabase-config.js

# Environment and secrets (other)
.env
.env.local
.env.*.local
*.pem
.env.local
.env.*.local
*.pem

# OS and editor
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/settings.json

# Optional: uncomment if you add tooling later
# node_modules/
# dist/
# .cache/
